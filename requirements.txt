Flask==3.0.2

python-dotenv==1.0.1
# numpy==1.26.4
# pandas==2.2.1 # Pandas is available by default in AWS Lambda
# yfinance==0.2.37 # Disable in prod, to reduce function size, since it is not used
sqlalchemy-libsql==0.1.0
cvxopt==1.3.2